{
  "name": "tanstack-react-modules",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "description": "Modular React framework for building composable frontend systems with TanStack Router and zustand.",
  "repository": {
    "type": "git",
    "url": "https://github.com/kibertoad/reactive.git"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter shell dev",
    "test": "vitest run",
    "test:e2e": "pnpm --filter @tanstack-react-modules/cli test:e2e",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^4.1.0"
  }
}
